{
  "installCommand": "yarn install",
  "buildCommand": "DISABLE_ESLINT_PLUGIN=true yarn build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/((?!static|favicon|manifest|robots|logo).*)", "destination": "/index.html" }
  ]
}